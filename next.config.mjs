/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['https://ik.imagekit.io', 'lh3.googleusercontent.com', 'avatars.githubusercontent.com']
    },
    reactStrictMode: false
};

export default nextConfig;
